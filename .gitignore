.claude/
.DS_Store

# Real secrets — never upload. Only .env.example is tracked.
.env
.env.local

# local scratch files from ad-hoc rep-day audits (never commit)
scratch_*.py
scratchpad_*.json

# generated reports (per-rep performance data — lives in Supabase, not git history)
reports/
